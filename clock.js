import React, {Component} from 'react';
import {
    Platform,
    StyleSheet,
    View,
    Text,
    TouchableNativeFeedback
} from 'react-native';

const styles = StyleSheet.create({
    // style for the container for the page
    container: {
        flex: 1
    }
});
